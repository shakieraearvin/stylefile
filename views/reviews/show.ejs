<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= review.itemName %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= review.itemName %></h1>
    <% if (review.itemReview) { %>
    <p>Notes: <%= review.itemReview %></p>
    <% } %> <% if (review.itemLink) { %>
    <a href="<%= review.itemLink %>">View Style Review</a>
    <% } %>
    <p>Recommendation: <%= review.recommendation %></p>
    <form
      action="/users/<%= user._id %>/res/<%= review._id %>?_method=DELETE"
      method="POST"
    >
      <button type="submit">Delete this style file</button>
    </form>
  </body>
</html>
